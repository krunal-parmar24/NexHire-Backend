# Database (Supabase/Postgres)
DATABASE_CONNECTION_STRING=Host=<host>;Port=5432;Database=NexHire;Username=<user>;Password=<password>

# JWT
Jwt__SigningKey=replace-with-a-long-random-secret
Jwt__AccessTokenExpiryMinutes=15

# LLM (server-only)
LLM_API_KEY=replace-with-your-llm-key
